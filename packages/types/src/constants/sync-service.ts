import { FlowStatus } from '../enums/features/flow-status.enum';

export const COMPLETE_FLOW_STATUS = [FlowStatus.Completed.toString(), FlowStatus.Failed.toString(), FlowStatus.Cancelled.toString()];
